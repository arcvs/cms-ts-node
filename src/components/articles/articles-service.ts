
export const createItem = (text: string): string => {
  return text
}

export const readItem = (text: string): string => {
  return text
}

export const updateItem = (text: string): string => {
  return text
}

export const deleteItem = (text: string): string => {
  return text
}

